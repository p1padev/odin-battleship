// ES6 code snippet
export const greet = (name) => `Hello, ${name}! Welcome to ES6.`;

const person = {
  firstName: 'John',
  lastName: 'Doe',
  age: 30,
};

const { firstName, lastName, age } = person;
